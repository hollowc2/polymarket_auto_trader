# Polymarket Bot Configuration

# Your Polygon wallet private key (for signing orders)
PRIVATE_KEY=0x_your_private_key_here

# Strategy settings
STREAK_TRIGGER=4          # Bet after N consecutive same outcomes
BET_AMOUNT=5              # USD per bet (min 5 on Polymarket)
MAX_DAILY_BETS=50         # Max bets per day
MAX_DAILY_LOSS=50         # Stop trading if daily loss exceeds this

# Timing
ENTRY_SECONDS_BEFORE=30   # Enter N seconds before window opens

# Mode
PAPER_TRADE=true          # Set to false for live trading

# File paths — override these when running in Docker (matches docker-compose.yml volumes)
# TRADES_FILE=/app/state/trades.json
# HISTORY_FILE=/app/state/trade_history_full.json
# LOG_FILE=/app/state/bot.log
