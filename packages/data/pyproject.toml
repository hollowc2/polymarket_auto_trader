[project]
name = "polymarket-algo-data"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = ["pandas>=3.0.0", "requests>=2.32.5", "pyarrow>=23.0.1"]

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"
